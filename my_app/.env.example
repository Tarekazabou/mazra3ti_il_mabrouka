# Voice Chat Configuration - Example
# Copy this file to .env and fill in your actual values
# Never commit .env file with real credentials to git!

# Gemini API Key
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# LiveKit Server URL
# For LiveKit Cloud: wss://your-project.livekit.cloud
# For self-hosted: wss://your-server.com
LIVEKIT_URL=wss://your-livekit-server.com

# LiveKit Access Token
# Generate from your LiveKit server or use the CLI
# In production, generate tokens from your backend, not hardcode them
LIVEKIT_TOKEN=your-livekit-token-here

# LiveKit Room Name (optional, default: farm-voice-assistant)
LIVEKIT_ROOM=farm-voice-assistant

# To use these variables when running the app:
# flutter run --dart-define=GEMINI_API_KEY=$GEMINI_API_KEY --dart-define=LIVEKIT_URL=$LIVEKIT_URL --dart-define=LIVEKIT_TOKEN=$LIVEKIT_TOKEN

# Or build the app with:
# flutter build apk --dart-define=GEMINI_API_KEY=$GEMINI_API_KEY --dart-define=LIVEKIT_URL=$LIVEKIT_URL --dart-define=LIVEKIT_TOKEN=$LIVEKIT_TOKEN
